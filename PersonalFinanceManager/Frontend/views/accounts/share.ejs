<div class="row mt-5">
    <div class="col-md-6 m-auto">
        <div class="card card-body bg-secondary text-light">
            <h1 class="text-center mb-3 text-light">Udostępnij Konto: <%= account.name %></h1>
            <p class="text-center text-light">Udostępnij dostęp do swojego konta innemu użytkownikowi podając jego adres e-mail.</p>
            
            <hr class="bg-light">

            <form action="/accounts/share/<%= account.id %>" method="POST">
                <div class="mb-3">
                    <label for="email" class="form-label">Adres E-mail Użytkownika</label>
                    <input
                        type="email"
                        id="email"
                        name="email"
                        class="form-control"
                        placeholder="Wpisz e-mail osoby, której chcesz udostępnić konto"
                        required
                    />
                </div>

                <div class="mb-3">
                    <label for="accessLevel" class="form-label">Poziom Dostępu</label>
                    <select id="accessLevel" name="accessLevel" class="form-select" required>
                        <option value="read">Tylko odczyt (widok transakcji i salda)</option>
                        <option value="write">Odczyt i Zapis (możliwość dodawania transakcji)</option>
                    </select>
                </div>

                <div class="d-grid gap-2 mb-3">
                    <button type="submit" class="btn btn-info text-white">Udostępnij Konto</button>
                    <a href="/accounts/edit/<%= account.id %>" class="btn btn-secondary">Powrót do Edycji</a>
                </div>
            </form>
        </div>
    </div>
</div>